import {Link, useParams} from 'react-router-dom'; import {services,gallery} from '../data.js';
const imgSet=src=>`${src.replace('.webp','-sm.webp')} 560w, ${src} 1100w`;
export default function ServiceDetail(){const {slug}=useParams(); const s=services.find(x=>x.slug===slug); if(!s)return <main className="notfound"><h1>Service introuvable</h1><Link to="/services">Retour aux services</Link></main>;return <main><section className="detail-hero"><img src={s.img} srcSet={imgSet(s.img)} sizes="100vw" alt={s.title} decoding="async"/><div className="detail-overlay"/><div className="detail-copy"><span className="section-kicker light">D.M. MAÇONNERIE</span><h1>{s.title}</h1><p>{s.desc}</p><Link className="btn beige" to="/contact">Demander un devis →</Link></div></section><section className="section detail-body"><div><span className="section-kicker">PRESTATION</span><h2>Un travail adapté<br/>à votre chantier.</h2><p>{s.desc} Avant toute intervention, l’objectif est de comprendre précisément le besoin et de proposer une solution cohérente avec l’existant.</p></div><div className="bullet-panel">{s.bullets.map(x=><div key={x}><span>✓</span><b>{x}</b></div>)}</div></section><section className="section compact-gallery"><div className="section-head"><div><span className="section-kicker">EN IMAGES</span><h2>Quelques réalisations</h2></div><Link to="/realisations">Toute la galerie →</Link></div><div className="triple-grid">{gallery.slice(0,3).map((g,i)=><img src={g.src} srcSet={imgSet(g.src)} sizes="(max-width: 900px) calc(100vw - 30px), 33vw" alt={g.title} loading="lazy" decoding="async" key={i}/>)}</div></section></main>}
